import React from "react";
import { styled } from "../stitches.config";
/**
 * Box is a low-level utility component on top of which all other components are built. It renders a div element by default and applies CSS properties directly via props.
 */
const Box = styled("div");

export default Box;
